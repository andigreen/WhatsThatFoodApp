<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_password_recovery"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.wtf.whatsthatfoodapp.auth.PasswordRecoveryActivity">

    <TextView
        android:text="Enter your email and then click recover"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/Recovery"
        android:layout_marginBottom="42dp"
        android:layout_above="@+id/emailfield"
        android:layout_centerHorizontal="true" />

    <Button
        android:text="Recover"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/recover_btn"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="196dp" />

    <TextView
        android:text="Email:"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/emailtext"
        android:layout_marginBottom="28dp"
        android:layout_above="@+id/recover_btn"
        android:layout_alignLeft="@+id/Recovery"
        android:layout_alignStart="@+id/Recovery" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="textEmailAddress"
        android:ems="10"
        android:id="@+id/emailfield"
        android:layout_marginLeft="18dp"
        android:layout_marginStart="18dp"
        android:layout_alignBottom="@+id/emailtext"
        android:layout_toRightOf="@+id/emailtext"
        android:layout_toEndOf="@+id/emailtext" />

    <Button
        android:text="Log in"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignLeft="@+id/recover_btn"
        android:layout_alignStart="@+id/recover_btn"
        android:layout_marginBottom="74dp"
        android:id="@+id/login_btn" />

    <Button
        android:text="Create an account"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="17dp"
        android:id="@+id/signup_btn"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true" />

</RelativeLayout>
